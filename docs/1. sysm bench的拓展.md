### 1. sysm bench的拓展

数据集基于教学材料，短文较多，中长文较少，总共151个案例，车辆交通领域就占 108 个案例，其他领域样本明显较少

### 2. sysm bench复现

为什么加入COT之后效果反而变差，

### 3. sysmEval评测指标

基于大模型（GPT4）分割原子语义，并判断生成结果与目标结果的原子语义是否对应，得到precision、recal、F1值，结合传统的NLP领域指标（BLUE、ROUGE、BertScore）用于评估生成效果。

依赖大模型进行语义判断，是否可靠，多次重复实验是否结果相同

没有做语法检查

